## 2026-07-16 — 音乐源导入功能全新搭建 + 旧源清理

### 新模块架构 `lib/music_source/`

**核心层 (core/)**
- `lx_runtime.dart` — JS运行时环境注入（polyfills、CryptoJS、LX API、httpFetch）
- `lx_bridge.dart` — 脚本桥接器（QuickJS→搜索/URL/歌词）
- `track_adapter.dart` — MusicTrack ↔ Song 适配器

**模型/服务/Provider/UI**
- 完整分层：models → services → providers → screens
- 音源中心 (`/source-hub`)、测试工具 (`/source-test`)

### 旧源清理
- 删除: source_engine.dart, source_api.dart, built_in_sources.dart, sample_scripts.dart, pdone_latest.js, source_engine_sixyin_test.dart
- 重写: source_provider.dart, search_provider.dart, player_provider.dart — 全部委托给新模块
- 更新: settings_screen.dart, test文件

### 编译状态
- `flutter analyze` 零错误零警告，6个info级提示
